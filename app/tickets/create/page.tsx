import CreateTicket from "./createTicket";

//server side
export default () => {
  return (
    <div>
      Create a new Ticket
      {/* client side component */}
      <CreateTicket />
    </div>
  );
};
